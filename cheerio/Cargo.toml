[package]
name = "cheerio"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["Window", "Storage"] }

[profile.release]
opt-level = "s"
lto = true
